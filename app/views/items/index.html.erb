<h2>To Do List</h2>
<h3>“Never put off till tomorrow what may be done day after tomorrow just as well.”</h3>

  <div class="row">
    <div class="col-md-6">


      <% @items.each do |item| %>
        <div class="media">
          <div class="media-body">
       <h4 class="media-heading">
         <p><%= item.name %></p>
       </h4>
     </div>
    </div>
  </div>
 </div>
   <% end %>


  <div class="row">
    <div class="col-md-4">
      <%= link_to "New Item", new_item_path, class: 'btn btn-success' %>
    </div>
  </div>
